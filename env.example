# ===========================================
# UCS INDEX PLATFORM - CONFIGURAÇÃO DE AMBIENTE
# ===========================================
# Copie este arquivo para .env.local e preencha com suas credenciais

# ===========================================
# FIREBASE CONFIGURATION (CLIENT-SIDE)
# ===========================================
# Obtenha estas informações no Firebase Console > Project Settings > General
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# ===========================================
# FIREBASE ADMIN (SERVER-SIDE)
# ===========================================
# Service Account JSON codificado em Base64
# Para gerar: Firebase Console > Project Settings > Service Accounts > Generate New Private Key
# Depois execute: cat service-account.json | base64 -w 0
FIREBASE_SERVICE_ACCOUNT_BASE64=base64_encoded_service_account_json_here

# ===========================================
# GOOGLE AI CONFIGURATION
# ===========================================
# Obtenha no Google AI Studio: https://aistudio.google.com/app/apikey
GOOGLE_AI_API_KEY=your_google_ai_api_key_here

# ===========================================
# N8N INTEGRATION
# ===========================================
# URL do webhook N8N para recálculos e automações
N8N_WEBHOOK_URL=https://your_n8n_instance.com/webhook/ucs
N8N_API_KEY=your_n8n_api_key_here

# ===========================================
# APPLICATION SETTINGS
# ===========================================
# URL da aplicação (desenvolvimento/produção)
NEXT_PUBLIC_APP_URL=http://localhost:9002

# Ambiente (development/production)
NODE_ENV=development

# ===========================================
# OPCIONAL: CONFIGURAÇÕES AVANÇADAS
# ===========================================
# Cache TTL em segundos (padrão: 300)
CACHE_TTL=300

# Rate limiting (requests por minuto)
RATE_LIMIT_MAX_REQUESTS=100

# Debug mode (true/false)
DEBUG_MODE=false

# ===========================================
# OPCIONAL: MONITORAMENTO
# ===========================================
# Sentry DSN para monitoramento de erros (opcional)
# SENTRY_DSN=your_sentry_dsn_here

# ===========================================
# OPCIONAL: EMAIL/SMTP
# ===========================================
# Configurações de email para notificações (opcional)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password

# ===========================================
# INSTRUÇÕES DE CONFIGURAÇÃO
# ===========================================
# 1. Copie este arquivo: cp env.example .env.local
# 2. Preencha todas as variáveis obrigatórias
# 3. Para produção, configure as mesmas variáveis no painel do seu provedor
# 4. NUNCA commite o arquivo .env.local no Git
# 5. Mantenha suas chaves de API seguras e rotacione periodicamente

# ===========================================
# TROUBLESHOOTING
# ===========================================
# - Erro de Firebase: Verifique se as chaves estão corretas
# - Erro de Google AI: Verifique se a API key está ativa
# - Erro de N8N: Verifique se o webhook está acessível
# - Erro de build: Verifique se todas as variáveis NEXT_PUBLIC_ estão configuradas
